#include <bits/stdc++.h>

using namespace std;

int n, k, nr = 1;

int main()
{
    cin >> n;
    
    while(n > 0)
    {
        if((n & 1) == 1)
        {
            cout << (nr << k) << " ";
        }
        
        k ++;
        n = n >> 1;
    }

    return 0;
}
